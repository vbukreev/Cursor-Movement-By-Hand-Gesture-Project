This worked with justthe camera and the pixel identifiacations
# import cv2
# import mediapipe as mp

# # Initialize Mediapipe Hands module
# mp_hands = mp.solutions.hands
# hands = mp_hands.Hands(min_detection_confidence=0.7, min_tracking_confidence=0.5)
# mp_drawing = mp.solutions.drawing_utils

# # Initialize the webcam
# cap = cv2.VideoCapture(0) 

# # Check if the webcam is opened correctly
# if not cap.isOpened():
#     print("Error: Could not open webcam.")
#     exit()

# while True:
#     # Read the current frame from the webcam
#     ret, frame = cap.read()

#     # If we successfully get a frame, process it
#     if ret:
#         # Flip the frame horizontally for a mirror effect
#         frame = cv2.flip(frame, 1)

#         # Convert the frame to RGB (Mediapipe needs RGB images)
#         rgb_frame = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)

#         # Process the frame and get the hand landmarks
#         results = hands.process(rgb_frame)

#         # If hand landmarks are found
#         if results.multi_hand_landmarks:
#             for landmarks in results.multi_hand_landmarks:
#                 # Draw landmarks on the frame
#                 mp_drawing.draw_landmarks(frame, landmarks, mp_hands.HAND_CONNECTIONS)

#         # Display the processed frame
#         cv2.imshow('Hand Gesture Detection', frame)

#     # Wait for a key press to exit the loop (press 'q' to quit)
#     if cv2.waitKey(1) & 0xFF == ord('q'):
#         break

# # Release the webcam and close any OpenCV windows
# cap.release()
# cv2.destroyAllWindows()


-----------------------
This worked with the cursor movement as well as the camera with pixel identifiacations


import cv2
import mediapipe as mp
import pyautogui

# Initialize Mediapipe Hands module
mp_hands = mp.solutions.hands
hands = mp_hands.Hands(min_detection_confidence=0.7, min_tracking_confidence=0.5)
mp_drawing = mp.solutions.drawing_utils

# Initialize the webcam
cap = cv2.VideoCapture(0)

# Check if the webcam is opened correctly
if not cap.isOpened():
    print("Error: Could not open webcam.")
    exit()

# Define screen size
screen_width, screen_height = pyautogui.size()

while True:
    # Read the current frame from the webcam
    ret, frame = cap.read()

    # If we successfully get a frame, process it
    if ret:
        # Flip the frame horizontally for a mirror effect
        frame = cv2.flip(frame, 1)

        # Convert the frame to RGB (Mediapipe needs RGB images)
        rgb_frame = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)

        # Process the frame and get the hand landmarks
        results = hands.process(rgb_frame)

        # If hand landmarks are found
        if results.multi_hand_landmarks:
            for landmarks in results.multi_hand_landmarks:
                # Get the x, y position of the wrist (landmark 0)
                wrist = landmarks.landmark[mp_hands.HandLandmark.WRIST]
                
                # Normalize the coordinates to the screen size
                x = int(wrist.x * screen_width)
                y = int(wrist.y * screen_height)
                
                # Move the cursor to the detected position
                pyautogui.moveTo(x, y)
                
                # Draw landmarks and connections on the frame
                mp_drawing.draw_landmarks(frame, landmarks, mp_hands.HAND_CONNECTIONS)

        # Display the processed frame
        cv2.imshow('Hand Gesture Control', frame)

    # Wait for a key press to exit the loop (press 'q' to quit)
    if cv2.waitKey(1) & 0xFF == ord('q'):
        break

# Release the webcam and close any OpenCV windows
cap.release()
cv2.destroyAllWindows()


`````````````````````````````````````````````````````````
